import { ClipboardPenLine, Palette, Send, Share2 } from "lucide-react";
import { Reveal } from "./Reveal";
const steps = [["01", "Choose your package", "Find the perfect fit for your celebration.", ClipboardPenLine], ["02", "Send us the details", "Tell us the who, where and when.", Send], ["03", "We create the magic", "We design a site uniquely yours.", Palette], ["04", "Share your love", "Send your link and let guests RSVP.", Share2]];
export function HowItWorks() { return <section className="how section" id="how-it-works"><Reveal><p className="kicker">Simple as can be</p><h2>From “yes” to <em>“I do”</em><br />in four lovely steps.</h2></Reveal><div className="steps">{steps.map(([num, title, text, Icon], i) => { const StepIcon = Icon as typeof ClipboardPenLine; return <Reveal delay={i * .12} key={num as string} className="step"><div className="step-icon"><StepIcon size={24} /></div><span className="step-num">{num as string}</span><h3>{title as string}</h3><p>{text as string}</p></Reveal>})}</div></section>; }

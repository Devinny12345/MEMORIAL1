import { CalendarDays, Camera, Clock3, Heart, MapPin, MessageCircleHeart, Music2, QrCode, Send, Smartphone, TicketCheck, UsersRound } from "lucide-react";
import { Reveal } from "./Reveal";
const features = [[TicketCheck, "RSVP management"], [Clock3, "Countdown timer"], [MapPin, "Google Maps"], [Camera, "Photo gallery"], [Heart, "Our Story"], [CalendarDays, "Event schedule"], [UsersRound, "Gift registry"], [Music2, "Song requests"], [MessageCircleHeart, "Guestbook"], [Smartphone, "Made for mobile"], [QrCode, "QR invitations"], [Send, "WhatsApp sharing"]];
export function Features() { return <section className="features section"><Reveal className="feature-head"><p className="kicker">Thoughtful by design</p><h2>Everything you need,<br /><em>nothing you don&apos;t.</em></h2><p>Your wedding website should do more than look lovely. It should make the whole experience effortless.</p></Reveal><div className="feature-grid">{features.map(([Icon, title], i) => { const FeatureIcon = Icon as typeof TicketCheck; return <Reveal delay={(i % 4) * .06} key={title as string}><div className="feature"><FeatureIcon size={21} /><span>{title as string}</span></div></Reveal>})}</div></section>; }

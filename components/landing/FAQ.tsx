"use client";
import { ChevronDown } from "lucide-react";
import { useState } from "react";
import { Reveal } from "./Reveal";
const questions = [["How long does it take?", "Most websites are ready within 5–7 days after we receive your wedding details. Need it sooner? Our Luxury package offers priority delivery."], ["Can I edit it later?", "Of course. We can make small updates as your plans come together, and Premium and Luxury packages include an easy editing handoff."], ["How do guests RSVP?", "Your guests tap the RSVP button, choose their response and dietary preferences, and their details arrive in your private RSVP list."], ["Does it work on phones?", "Beautifully. Every Say Yes Belize website is designed mobile-first, so it looks lovely wherever your guests open it."], ["Do you offer custom domains?", "Yes. A custom domain is included with Luxury, and can be added to any package." ]];
export function FAQ() { const [open, setOpen] = useState(0); return <section className="faq section"><Reveal className="center"><p className="kicker">Your questions, answered</p><h2>Good things to <em>know.</em></h2></Reveal><div className="faq-list">{questions.map(([q, a], i) => <Reveal key={q}><div className={`faq-item ${open === i ? "open" : ""}`}><button onClick={() => setOpen(open === i ? -1 : i)} aria-expanded={open === i}><span>{q}</span><ChevronDown /></button>{open === i && <p>{a}</p>}</div></Reveal>)}</div></section>; }
